using CommandLine;

namespace Backend.DatabaseAccess
{
    //class CommandLineOptions
    //{
    //    [Option('c', "clean", Required = false, Default = false, HelpText = "Remove all tables from database before migration.")]
    //    public bool IsClean { get; set; }
    //}
}
